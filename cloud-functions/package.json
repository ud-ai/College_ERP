{
  "name": "college-light-erp-functions",
  "private": true,
  "engines": { "node": "18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth,storage --project college-erp-dev",
    "build": "echo 'no build'",
    "lint": "echo 'no lint'",
    "test": "node tests/rules.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.2"
  }
}


